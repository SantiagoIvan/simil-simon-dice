class AppConfig {
  // Duración del splash screen
  static const Duration splashDuration = Duration(seconds: 2);

  // Animación de pulso
  static const Duration splashPulseDuration = Duration(milliseconds: 600);

  static const double splashPulseScale = 1.2;

  // Branding
  static const String appName = 'Simon dice';
}
